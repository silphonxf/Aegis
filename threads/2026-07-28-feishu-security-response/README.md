# 2026-07-28 飞书安全响应进度

## 今日目标

把 Aegis 的 IP 威胁研判和山石地址簿封禁接入飞书卡片，支持单聊、群聊、张晓帆单人授权、济南 IP 二次确认、一台山石测试设备和永久封禁。

## 已完成

- 新增飞书用户身份登记与 Aegis 用户绑定。
- 查询权限和封禁权限分离；封禁权限只允许绑定有效管理员。
- 新增持久化封禁批次、IP 明细、30 分钟过期和飞书事件幂等表。
- 新增建议封禁、全部恶意、济南二次确认、最终演练/永久封禁状态机。
- 新增飞书 WebSocket 长连接桥、单聊/群聊 @ 规则和交互卡片。
- 新增管理端飞书身份权限配置区域。
- 山石配置支持多目标字段；本阶段默认 `test-primary`。
- 山石批量封禁改为单次登录/读取/更新/回读，并增加目标级互斥锁。
- 新增成功、失败、演练和身份授权审计记录。
- 增加数据库迁移、环境变量示例、部署与测试文档。
- App Secret 没有写入代码或配置；聊天中暴露过的 Secret 必须重置。

## 明确保留的规则

- 封禁永久有效，不设置 TTL。
- 济南 IP 必须二次确认。
- 不要求申请人与审批人分离。
- 不按显示姓名授权，以飞书 open_id + Aegis 管理员绑定为准。
- 本轮不操作第二台山石设备。

## 上线前仍需人工完成

- 在飞书后台重置 App Secret，并把新值通过服务器环境变量注入。
- 开通机器人消息、发消息、卡片更新权限，订阅消息与卡片事件并发布应用版本。
- 张晓帆先给机器人发消息，管理员再从管理端登记的 open_id 授权。
- 在管理端核对 `test-primary` 的山石地址、账号、地址簿和测试目标标志。
- 先做飞书演练，不修改防火墙；真实测试需选择经批准且不会影响业务的 IP。
- 当前没有自动解封功能，误封只能人工从山石地址簿移除。

## 验证记录

最终测试与代码审查结果在本任务结束前补充到同目录的 `review.md`。
