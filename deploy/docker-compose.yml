version: "3.9"
services:
  aegis-api:
    build:
      context: ../backend
      dockerfile: Dockerfile
    container_name: aegis-api
    ports:
      - "8000:8000"
    env_file:
      - ../backend/.env
    restart: unless-stopped
